# Not working - example from http://www.activeadmin.info/docs/8-custom-actions.html
table_for assigns[:ticket].comments do
  column :id
  column :author
  column :body do |comment|
    simple_format comment.body
  end
end
action_item :only => :show do
  link_to('View on site', ticket_path(ticket)) if ticket.published?
end
